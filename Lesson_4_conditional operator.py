# Задача 1 (Оператор if)
# Дана переменная:

# temperature = 18
# Напиши условие: если temperature меньше 20, выведи строку "Надень куртку".

temperature = 18
print( "Надень куртку" if temperature < 20 else "нет")

# Задача 2 (Оператор else)
# Дана переменная:
# age = 15
# Напиши программу, которая:
# Проверяет, больше ли age 18,
# Если да — выводит "Доступ разрешён",
# Если нет — выводит "Доступ запрещён".

age = 15
print("Доступ разрешён" if age > 18  else "Доступ запрещён")

# Задача 3 (Оператор elif)
# Дана переменная:
# score = 85
# Напиши программу, которая выводит оценку по правилам:
# 90 и больше: "Отлично"
# от 75 до 89: "Хорошо"
# от 60 до 74: "Удовлетворительно"
# меньше 60: "Неудовлетворительно"

score = 85
if score >= 90:
    print("Отлично")
elif score >= 75:
    print("Хорошо")
elif score >= 60:
    print("Удовлетворительно")
else:
    print("Неудовлетворительно")
# Задача 4 (Логические операции)

# Даны две переменные:
# has_ticket = True
# age = 12
# Напиши программу, которая проверяет:
# Есть ли билет (has_ticket) И возраст (age) больше или равен 12,
# Если оба условия верны — выводи "Проход разрешён",
# Если хотя бы одно неверно — выводи "Проход запрещён".
has_ticket = True
age = 12
if has_ticket and age >= 12:
    print("Проход разрешён")
else:
    print("Проход запрещён")


# Задача 5 (Комбинация логических операторов)
# Даны три переменные:
# is_member = True
# purchase_amount = 1200
# has_coupon = False
# Напиши программу, которая проверяет:
# Если покупатель является участником программы (is_member) ИЛИ сумма покупки (purchase_amount) больше 1000,
# И при этом у него нет купона (has_coupon == False),
# То выводи "Скидка 10%",
# Иначе — "Скидка 0%".

is_member = True
purchase_amount = 1200
has_coupon = False

if (is_member or purchase_amount > 1000) and not has_coupon:
    print("Скидка 10%")
else:
    print("Скидка 0%")

# Задача 6 (Тернарный оператор + логические операции)
# Даны две переменные:
# temperature = 28
# is_weekend = True
# Напиши одну строку кода с тернарным оператором, которая:
# Если температура выше 25 И это выходной (is_weekend), выводит "Идём купаться",
# Иначе выводит "Сидим дома".

temperature = 28
is_weekend = True
if is_weekend and temperature > 25:
    print("Идём купаться")
else:
    print("Сидим дома")

# Задача 7 (Приоритет логических операций)
# Даны три переменные:
# has_key = True
# is_night = False
# is_emergency = True
# Напиши условие, которое:
# Разрешает доступ, если:
# Есть ключ (has_key) И не ночь (not is_night), ИЛИ
# Это экстренный случай (is_emergency),
# Выводит "Доступ разрешён" или "Доступ запрещён".

has_key = True
is_night = False
is_emergency = True
if (has_key and not is_night) or is_emergency:
    print("Доступ разрешён")
else:
    print("Доступ запрещён")

# Задача 8 (Комбинированные условия + ввод данных)
# Пользователь вводит два значения:
# age = int(input("Введите ваш возраст: "))
# has_license = input("Есть ли у вас права (да/нет)? ") == "да"
# Напиши программу, которая:
# Проверяет, что возраст от 18 И есть права,
# ИЛИ возраст от 16 И есть права И есть сопровождающий (добавь переменную with_parent),
# Выводит "Можно водить" или "Нельзя водить".

age = int(input("Введите ваш возраст: "))
has_license = input("Есть ли у вас права (да/нет)? ") == "да"
with_parent = input("Есть ли у вас сопровождающий (да/нет)? ") == "да"
if (has_license and age >= 18) or (with_parent and has_license and age >= 16):
    print("Можно водить")
else:
    print("Нельзя водить")

# Задача 9 (Обработка исключений + условия)
# Пользователь вводит два числа:
# try:
#     num1 = int(input("Введите первое число: "))
#     num2 = int(input("Введите второе число: "))
# except ValueError:
#     print("Ошибка: вводите только числа!")
# else:
#     # Ваш код с условиями ниже
# Напиши программу, которая:
# Проверяет, что оба числа положительные,
# Если да — выводит их сумму,
# Если нет — выводит "Числа должны быть положительными",
# При вводе нечисловых данных показывает ошибку (уже реализовано в шаблоне).
try:
     num1 = int(input("Введите первое число: "))
     num2 = int(input("Введите второе число: "))
except ValueError:
    print("Ошибка: вводите только числа!")
else:
     if num1 > 0 and num2 > 0:
         print(num1+num2)
     else:
         print("Числа должны быть положительными")

# Задача 10 (Финал темы: комплексные условия)
#
# Даны три переменные:

# is_rainy = False
# temperature = 22
# is_weekend = True
# Напиши программу, которая решает:
# Если не дождь (not is_rainy) И температура от 18 до 30:
# В выходные (is_weekend) → "Идём гулять",
# В будни → "Можно гулять, но недолго",
# Иначе → "Сидим дома",
# Добавь проверку на экстремальную температуру (<0 или >35) → "Опасно для здоровья!".

is_rainy = False
temperature = 22
is_weekend = True

if (18 <= temperature <= 30) and not is_rainy:
    if is_weekend:
        print("Идём гулять")
    else:
        print("Можно гулять, но недолго")
elif (temperature < 0) or temperature >35:
    print("Опасно для здоровья!")
else:
    print("Сидим дома")
